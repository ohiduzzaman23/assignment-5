1. Answer
getElementById()
Works with: Searches element only by id
getElementsByClassName()
Works with: Searches element by class name
querySelector()
Works with: Searches element using CSS Selector (id, class, tag, attribute — all can be used)

2. Answer
document.createElement("tagName")
appendChild()

3. Answer
Event Bubbling: An event starts from the target element and propagates upward through its parent elements.

4.Answer
Event Delegation: Attaching a single event listener to a parent to handle events on its child elements, improving efficiency and supporting dynamic elements.

5.Answer
preventDefault() stops the default browser action, while stopPropagation() stops the event from bubbling or capturing further.